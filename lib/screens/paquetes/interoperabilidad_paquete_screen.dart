import 'package:flutter/material.dart';

class InteroperabilidadPaqueteScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Interoperabilidad de Paquetes')),
      body: Center(child: Text('Pantalla de Interoperabilidad de Paquetes')),
    );
  }
}
