import 'package:flutter/material.dart';

class EntregasPaqueteScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Entregas de Paquetes')),
      body: Center(child: Text('Pantalla de Entregas de Paquetes')),
    );
  }
}
