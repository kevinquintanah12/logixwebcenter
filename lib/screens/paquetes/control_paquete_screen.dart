import 'package:flutter/material.dart';

class ControlPaqueteScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Control de Paquetes')),
      body: Center(child: Text('Pantalla de Control de Paquetes')),
    );
  }
}
