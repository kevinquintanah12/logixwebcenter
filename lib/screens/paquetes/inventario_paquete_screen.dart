import 'package:flutter/material.dart';

class InventarioPaqueteScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Inventario de Paquetes')),
      body: Center(child: Text('Pantalla de Inventario de Paquetes')),
    );
  }
}
