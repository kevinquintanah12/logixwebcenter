import 'package:flutter/material.dart';

class AlertasPaqueteScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Alertas de Paquetes')),
      body: Center(child: Text('Pantalla de Alertas de Paquetes')),
    );
  }
}
