import 'package:flutter/material.dart';

class AtencionPaqueteScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Atención a Paquetes')),
      body: Center(child: Text('Pantalla de Atención a Paquetes')),
    );
  }
}
